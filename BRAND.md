# فیت‌میت — برند بریف و سیستم رنگ

> این سند مبنای تصمیم‌های بصری اپ است. هر رنگ، فاصله و اندازه‌ای که در
> `assets/styles.css` تعریف شده از همین‌جا می‌آید.

## ۱. جایگاه برند (Positioning)

**فیت‌میت یار تمرین است، نه مربی پرسروصدا.**

اپ‌های تمرینی معمولاً در دو سر طیف می‌ایستند:

| قطب | مثال‌ها | زبان بصری | مشکل |
|---|---|---|---|
| «هیجان» | اپ‌های باشگاهی نئونی | مشکی + نئون تند، تایپ خیلی بزرگ، انیمیشن زیاد | زیر نور باشگاه خسته‌کننده و کم‌کنتراست می‌شود؛ همه‌چیز «مهم» به نظر می‌رسد |
| «دیتا» | اپ‌های ردیابی و ساعت‌های هوشمند | خاکستری سرد، نمودار زیاد | حین ست، کاربر وقت خواندن نمودار ندارد |

فیت‌میت وسط این دو می‌ایستد: **پایهٔ آرام و خوانا، با یک رنگ تأکید که فقط
یک معنی دارد — «کار بعدی‌ات این است».**

## ۲. کاربر و شرایط استفاده

سناریوی واقعی استفاده، همهٔ تصمیم‌های طراحی را تعیین می‌کند:

- گوشی روی زمین یا لبهٔ دستگاه است، **فاصلهٔ چشم تا صفحه ۶۰ تا ۹۰ سانتی‌متر**.
- دست عرق‌کرده، گاهی با دستکش → **هدف لمس حداقل ۴۸px**، دکمه‌های اصلی ۵۶px.
- نور باشگاه یا آفتاب پنجره → **کنتراست متن اصلی حداقل ۷:۱** (AAA) و متن فرعی حداقل ۴٫۵:۱.
- ضربان بالا، تمرکز پایین → در هر لحظه فقط **یک** تصمیم روی صفحه.
- تمرین دونفره روی یک گوشی → هویت هر ورزشکار باید **بدون خواندن متن** قابل تشخیص باشد.

## ۳. اصول طراحی

1. **یک رنگ = یک معنی.** رنگ تأکید فقط برای «اقدام بعدی» و «وضعیت فعال». هرگز برای تزئین.
2. **رنگ هیچ‌وقت تنها حامل معنا نیست.** نوبت ورزشکار = رنگ + حرف اول نام + برچسب متنی. برای کوررنگی و برای عکس‌گرفتن سیاه‌وسفید هم کار می‌کند.
3. **عدد قهرمان است.** وزنه، تکرار و زمان با فونت عددی tabular و درشت‌ترین اندازهٔ صفحه.
4. **بی‌حرکتی پیش‌فرض است.** انیمیشن فقط جایی که اطلاعات می‌دهد (تایمر، نمایش فرم حرکت) و همیشه تابع `prefers-reduced-motion`.
5. **راست‌چین اصیل.** اعداد و واحدهای لاتین داخل `bdi` تا ترتیب جمله نشکند.

## ۴. پالت رنگ

پالت قبلی (لیموییِ نئون روی مشکی) دو مشکل داشت: در حالت روشن اصلاً کار
نمی‌کرد، و چون رنگ تأکید همه‌جا استفاده شده بود، دیگر چیزی را «تأکید» نمی‌کرد.
پالت جدید بر پایهٔ یک خنثای سردِ گرافیتی + دو رنگ کارکردی است:

### رنگ‌های برند

| نقش | نام | تیره | روشن | چرا |
|---|---|---|---|---|
| تأکید اصلی | **تاید** (Tide) | `#22C7B0` | `#0E7C6E` | سبزآبی: هم انرژی دارد هم آرام است؛ در هر دو تم کنتراست کافی می‌دهد و با قرمز/سبزِ «خطا/موفقیت» اشتباه نمی‌شود |
| تأکید گرم | **امبر** (Ember) | `#FF8348` | `#C2410C` | شدت، تکنیک‌های سنگین، هشدار ملایم. مکمل مستقیم تاید روی چرخهٔ رنگ |
| ورزشکار ۱ | تاید | `#22C7B0` | `#0E7C6E` | همان تأکید اصلی |
| ورزشکار ۲ | **آیریس** (Iris) | `#A78BFA` | `#6D4AE0` | در کنار تاید هم در روشنایی و هم در فام تفکیک‌پذیر است |

### خنثی‌ها

پایهٔ سرد با ته‌رنگ آبی (`hue 215`) تا کنار تاید تمیز بنشیند:

```
تیره:  #0B0F14 → #111720 → #161D27 → #1E2733 → #27323F   (زمینه تا سطح ۳)
روشن:  #F5F7FA → #FFFFFF → #EEF1F5 → #E4E9F0             (زمینه تا سطح ۳)
```

### رنگ‌های وضعیت

`--ok #34D399` · `--danger #FF6B6B` · `--info #5AB0FF` — هر سه همیشه با آیکون یا متن همراه.

### نسبت استفاده

قانون **۶۰/۳۰/۱۰**: ۶۰٪ خنثای زمینه، ۳۰٪ سطوح و خطوط، **حداکثر ۱۰٪ رنگ تأکید**.
اگر در یک اسکرین‌شات بیش از دو عنصر تاید دیدید، یکی از آن‌ها اشتباه است.

## ۵. تایپوگرافی

- خانوادهٔ فارسی: `Vazirmatn` با فالبک سیستمی.
- اعداد: فونت مونو با `font-variant-numeric: tabular-nums` تا تایمر نلرزد.
- مقیاس: ۱۱ / ۱۲٫۵ / ۱۵ / ۱۷ / ۲۰ / ۲۶ / ۴۰ / ۵۶.
- وزن‌ها: ۵۰۰ متن، ۷۰۰ عنوان کوچک، ۸۰۰–۹۰۰ عدد قهرمان.

## ۶. تم

سه حالت در تنظیمات: **خودکار (هم‌راستا با سیستم)** · **روشن** · **تیره**.
تم روی `<html data-theme>` می‌نشیند و همهٔ رنگ‌ها فقط از توکن‌ها می‌آیند؛
هیچ رنگ هاردکدی در ویوها نیست.
